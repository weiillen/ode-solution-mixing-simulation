# Publication notes

- The submitted notebook is preserved **byte-for-byte**.
- Its public filename was shortened from `ode_bonus_project_112006265.ipynb` to `ode_bonus_project.ipynb` so the student number is not exposed in the repository filename.
- The assignment PDF is preserved **byte-for-byte**.
- `__MACOSX/` and `._*` files from the ZIP were omitted because they are macOS archive metadata, not project work.
- Images in `assets/` were extracted from the notebook's existing saved outputs. The notebook was not re-run or edited to create them.

See `ORIGINAL_FILE_MANIFEST.tsv` for SHA-256 verification.
